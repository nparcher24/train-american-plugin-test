import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nickparrish.trainamerican',
  appName: 'Train American Plugin',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
